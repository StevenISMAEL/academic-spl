apiVersion: v1
kind: Secret
metadata:
  name: PRODUCT_NAME-frontend-secret
  namespace: PRODUCT_NAME
type: Opaque
stringData:
  # APP_KEY de Laravel — generado con: php artisan key:generate --show
  # El pipeline CI/CD reemplaza LARAVEL_APP_KEY_PLACEHOLDER
  APP_KEY: "LARAVEL_APP_KEY_PLACEHOLDER"
